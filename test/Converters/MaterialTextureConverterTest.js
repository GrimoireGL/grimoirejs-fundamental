import test from 'ava';
import MaterialTextureConverter from '../../lib-es5/Converters/MaterialTextureConverter';

test('MaterialTextureConverter should convert collectly', (t) => {
    t.truthy( /*Write test here*/ );
});
