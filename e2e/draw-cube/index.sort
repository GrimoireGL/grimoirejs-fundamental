@Pass{
    FS_PREC(mediump,float)

    @import "basic-vert"

    #ifdef FS
    @MOUSE_POSITION
    uniform vec2 mouse;

    @MOUSE_STATE{}
    uniform vec2 state;
     void main(){
         gl_FragColor=vec4(mouse,state.x,1);
     }
    #endif
}