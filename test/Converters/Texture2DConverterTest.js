import "../TestInit";
import test from 'ava';
import Texture2DConverter from '../../lib-es5/Converters/Texture2DConverter';

test('StringConverter should convert collectly', (t) => {
    t.truthy( /*Write test here*/ );
});
